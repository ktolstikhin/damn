version: "3.9"

services:
  api:
    command: ./api -addr=:8000 -debug

  nginx:
    ports:
      - 8080:80
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
      - ./nginx/html:/var/www/html
